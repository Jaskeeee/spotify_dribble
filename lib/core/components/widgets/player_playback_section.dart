import 'package:flutter/material.dart';

class PlayerPlaybackSection extends StatelessWidget {
  const PlayerPlaybackSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}